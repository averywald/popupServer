import AccountController from './account.controller'
import HomeController from './home.controller'

// collect all controllers to be included in the application
export const controllers = [AccountController, HomeController]
